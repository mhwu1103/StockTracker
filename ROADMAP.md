# Roadmap

目前這個網站是「成交值排行榜」——回答的是「今天誰在榜上、誰進誰出、名次怎麼走」。
方向是往「**資金流向儀表板**」走：從「誰在榜上」到「錢往哪裡去」。

這份文件把想做的事整理成優先級。標記 ✅ 的已經做完，規格保留下來當作實作紀錄；
其餘照優先級往下走。

## 優先級總表

| | 項目 | 使用者的問題 | 工程量 | 需要新資料源 |
|---|---|---|---|---|
| ✅ | [資料新鮮度警示](#p0資料新鮮度警示) | 我看到的是今天的盤嗎？ | XS | 否 |
| ✅ | [大盤走勢與集中度](#p1大盤走勢與集中度) | 市場熱不熱？資金在集中還是擴散？ | S | 否 |
| ✅ | [個股全期間統計](#p1個股全期間統計) | 這檔是老面孔還是第一次來？ | S–M | 否 |
| ✅ | [產業別與資金流向](#p2產業別與資金流向) | 今天哪個族群在動？ | M–L | 是（TWSE 公司基本資料） |
| ✅ | [篩選與排序](#p3篩選與排序) | 為什麼榜上都是 ETF？ | S | 否 |
| ✅ | [自選股與客製推播](#p3自選股與客製推播) | 我在意的那幾檔有動靜嗎？ | M | 否 |
| P4 | [上櫃（TPEx）涵蓋](#p4上櫃tpex涵蓋) | 為什麼搜不到某檔櫃買股？ | M | 是（TPEx API） |
| P4 | [進榜後表現統計](#p4進榜後表現統計) | 新進榜之後歷史上通常怎麼走？ | M | 否 |
| P5 | [匯出與分享](#p5匯出與分享) | 我想把這份榜單帶走 | S | 否 |
| P5 | [週／月維度排行](#p5週月維度排行) | 這週累計誰最熱？這個月誰進榜最多次？ | M | 否 |

---

## P0：資料新鮮度警示

> ✅ 已完成。`paintChrome()` 在最新資料距今超過 4 個日曆日時，於頂部顯示紅底提醒。

**使用者的問題**：我現在看到的是今天的盤嗎？

現況：前端 `paintChrome()`（`docs/app.js`）只是安靜地把 `index.json` 的 `latest` 塞進日期下拉選單。
排程若停擺，畫面看起來完全正常，使用者會以為看到的是當天的資料。這是唯一一個「不做會給出錯誤資訊」的項目。

規格：

- meta 列在 `latest` 距今超過 1 個台北交易日時顯示醒目提示：`資料為 8/17，已 N 個交易日未更新`
- 判斷寫在 `paintChrome()` 裡，用 `index.updated`／`index.latest` 與瀏覽器當下時間比對，不需要後端
- 週末與國定假日會誤判成「未更新」，門檻抓寬一點（例如超過 3 個日曆日才提示），寧可少報不要多報

順帶要查的維運問題：`.github/workflows/daily.yml` 的 `cron: "30 8 * * 1-5"` 是否真的有在跑。
GitHub 會自動停用「60 天沒有任何活動」的 repo 上的排程工作流程，停用時不會有通知。

---

## P1：大盤走勢與集中度

> ✅ 已完成。`index.json` 新增 `top200Values`／`top10Values`，前端多了「大盤」分頁。
> 實測集中度確實有訊息量：前 10 大佔比在 21.9%～47.2% 之間擺盪。

**使用者的問題**：市場整體是熱是冷？錢是集中在少數幾檔，還是擴散到很多檔？

`index.json` 已經存了 485 天的 `marketValues`（每日大盤成交值），但前端只拿它顯示 meta 列上的一個數字。
畫成走勢就是市場溫度計；再加上「前 200 大佔大盤的比重」，就得到一條比任何個股都有訊息量的集中度曲線。

### 資料面（改動很小）

`build_history.py` 的主迴圈裡 `payload["stocks"]` 當下就在手上，順手加總即可：

```python
top200 = sum(s["value"] for s in payload["stocks"] if s["rank"] <= twse.STREAK_RANK)
top10  = sum(s["value"] for s in payload["stocks"] if s["rank"] <= 10)
top200_values.append(round(top200 / 1e8, 2))
top10_values.append(round(top10 / 1e8, 2))
```

寫進 `index.json` 兩個與 `dates` 平行的新陣列 `top200Values` / `top10Values`。
485 個交易日大約多 8 KB；`index.json` 是前端唯一用 `cache: 'reload'` 抓的檔案，這個量可以接受。

`build_history.py` 本來就每次由 `daily/` 全量重算，加完欄位跑一次就會補齊全部歷史，不需要重新回補。

### 前端：新增「大盤」分頁 `#/market`

- 路由註冊在 `docs/index.html` 的 tab 列與 `docs/app.js` 的 `render()`
- 三張圖，沿用既有的 `drawLine()` 與 `pills('span', ...)`（60／120／全部）：
  1. 大盤成交值走勢（億元）
  2. 前 200 大成交值合計走勢
  3. 集中度走勢——`top200Values[i] / marketValues[i]`、`top10Values[i] / marketValues[i]` 兩條線
- 頂部沿用 `.stat-grid`：當日大盤成交值、對比前一日 ±%、前 200 佔比、前 10 佔比

`drawLine()` 目前只吃單一 dataset，集中度雙線圖需要把它小改成接受 datasets 陣列
（比畫成兩張圖好，P2 的族群比較也會用到）。

---

## P1：個股全期間統計

> ✅ 已完成。個股頁拆成「近 N 個交易日」與「全期間紀錄」兩張卡。

**使用者的問題**：這檔是天天在榜的老面孔，還是今天第一次來？以前來過幾次？最好曾經到第幾名？

現況缺口：個股頁 `renderStock()`（`docs/app.js`）的「期間最佳」與「進前 200 天數」
**只算目前選定區間（預設 120 日）內**的資料——因為統計是在 `slice` 出 labels 之後才做的。
使用者會以為那是歷史紀錄，其實只是最近幾個月。

### 資料面：完全不用改 Python

`seriesFor(code)` 已經把所有年度的 `history/{year}.json` 合併成完整的 `byDate: Map<date, {rank, value}>`，
全期間資料本來就在記憶體裡，只是被區間切掉了。單檔最多 485 個點，前端計算成本可忽略。

### 新增指標

依 `state.index.dates` 的順序掃過 `byDate`，以 `TOP = 200` 為進榜門檻：

| 指標 | 算法 |
|---|---|
| 總進榜天數 | `byDate` 中 `rank <= 200` 的天數 ／ 全部交易日數 |
| 進榜次數（波段數） | `rank <= 200` 的連續區段數量 |
| 歷史最佳名次 | `min(rank)` 與發生日期 |
| 最長連續天數 | 最長區段長度與起訖日 |
| 上次掉出榜 | 最後一個已結束區段的下一個交易日 |
| 目前狀態 | 在榜中（連 N 天）／已掉榜 M 天 |

兩個容易踩到的邊界：

- `byDate` 含 rank 201–300 的日子（daily 存 300 名是為了判斷進出榜），
  統計「進榜」時必須過濾 `rank <= 200`，不能只看那天有沒有值
- 連續區段的相鄰性要用 `index.dates` 的**索引**判斷，不能用日曆日相減（週末與休市日會斷）
- 若某段連續紀錄一路連到 `index.dates[0]`，沿用既有 `streakLabel()` 的 `+` 標示慣例

### UI

個股頁的統計格拆成兩區：「本區間」（現有六格，標題明確寫出是哪段區間）
與「全期間」（新的六格）。區間 pills 只影響前者與圖表。

---

## P2：產業別與資金流向

> ✅ 已完成，但有一處與原規格不同：**沒有產出 `docs/data/sector/{year}.json`**。
> 族群頁只需要當日與比較基準日的資料，而這兩份 daily 檔前端本來就會載入，
> 直接在前端 join `industry.json` 就夠了，不必多存一份約 1 MB 的衍生資料，
> 也避免產業歸類規則在 Python 與 JS 兩邊各寫一次。
> 之後若要做「產業成交值的長期走勢圖」，再回來補這份檔案。

**使用者的問題**：今天是 AI 伺服器在動還是航運在動？

這是看排行榜的人腦中真正的問題，而不是「第 37 名是誰」。目前資料裡完全沒有產業欄位，
這是最大的資料缺口，也是 P0–P2 裡唯一需要接新資料源的項目。

### 新資料源

端點：`https://openapi.twse.com.tw/v1/opendata/t187ap03_L`（上市公司基本資料，1095 筆）。

實測結果：`產業別` 欄位是**數字代碼**（台積電是 `"24"`）而不是中文，所以
`scripts/fetch_industry.py` 內建一份代碼→中文對照表（逐一比對過樣本公司），
對不到的代碼會歸為「其他」並在執行時印出提醒。目前 33 個產業、1089 檔，
最新一個交易日的前 200 名 100% 都對得到。

`scripts/fetch_industry.py` → `docs/data/industry.json`：

```json
{"updated":"2026-08-29T16:30:00+08:00","map":{"2330":"半導體業","2317":"電腦及週邊設備業"}}
```

- 沿用 `twse.fetch_json()` 與 `twse.write_json()`，不需要新的抓取邏輯
- ETF／ETN（`00` 開頭）不在公司清單裡 → 統一歸類為 `ETF`
- 4 碼＋英文字母的特別股（如 `2887Y`）不在清單裡 → fallback 到去掉字母的母公司代碼
- 對不到的一律歸 `其他`，並在 log 印出數量，資料源改格式時才看得出來
- 內容變動極慢，`daily.yml` 每天跑一次即可；沿用既有「內容沒變就不重寫」的作法，git diff 不會有雜訊

### 產業欄位不寫進 `daily/*.json`

485 個 daily 檔全部重寫會產生 20 MB 的 git diff，而且產業別是「現況」屬性、不是當日快照。
改成前端載入 `industry.json` 之後 join，一次 HTTP 請求就解決。

聚合直接在前端做（見章節開頭的說明）。`industry.json` 抓不到時，
整站當作沒有產業資訊：族群頁顯示提示、排行頁不出現產業標籤與篩選。

⚠️ 要在 UI 註明：產業別是用「今日的分類」回溯套用到歷史資料（`build_history.py` 每次全量重算），
公司改變產業歸屬時，歷史數字會跟著變。

### 前端：新增「族群」分頁 `#/sector`

- 當日各產業一列：榜上檔數、成交值合計（億）、佔前 200 大比重、對比前 1／5／20 日的變化
  （沿用既有的 `BASELINES` pills）
- 點一列展開該產業當日在榜的個股（沿用 `stockRow()`）
- 排行頁每一列加上產業標籤，並支援「只看某產業」

誠實標示：這是官方產業分類（「半導體業」「電子零組件業」），
不等於市場口中的「AI 伺服器」「重電」等題材族群。

---

## P3：篩選與排序

> ✅ 已完成。排行頁三個下拉：篩選（全部／只看自選／排除 ETF／各產業）、
> 排序（依名次／漲跌幅／名次變化）、成交值門檻（不限／50／100／200 億）。
> 純前端，全部改在 `renderRank()` 裡。

## P3：自選股與客製推播

> ✅ 已完成。個股頁有「加入自選」按鈕，清單存在 localStorage（key 為
> `stocktracker.watch`，讀寫都包在 try/catch 裡，無痕模式不會壞）。
> 自選股在所有列表上都標星號，排行頁可以只看自選。
>
> `notify_telegram.py` 新增 `WATCHLIST` 環境變數與 `--watchlist` 參數：
> 設了就只推那幾檔的進出榜動態，當天沒動靜就不發。
>
> ⚠️ 瀏覽器的 localStorage 到不了 GitHub Actions，兩邊**不會自動同步**。
> 排行頁選「只看自選」時會列出可複製的代號字串，手動貼進 secret。
> 要真的同步就得有後端、或把清單 commit 進 repo，兩者都不值得為此破壞靜態架構。

## P4：上櫃（TPEx）涵蓋

目前只涵蓋上市。使用者搜不到熱門櫃買股時，會直接認為程式壞了。

擴充路徑：在 `scripts/twse.py` 增加一個抓取函式並沿用 `make_record()`，其餘流程不必改。
要先決定上市與上櫃是**合併排名**還是**分成兩個榜**——這會影響 streak 的定義與所有歷史資料的意義。

## P4：進榜後表現統計

已經有 485 個交易日的全市場排名與收盤價，可以統計新進榜的股票在 +1／+5／+20 日的價格變化分布，
以及「站穩 5 天」與「一日行情」的差異。這是別的網站沒有的東西。

⚠️ 呈現方式必須是**歷史統計描述**，不能寫成訊號或建議。此頁需要再次標示「資料僅供參考，非投資建議」。

## P5：匯出與分享

CSV 下載（前端組字串 + Blob，不需要後端）、分享連結按鈕。目前完全沒有帶走資料的方法。

## P5：週／月維度排行

目前所有維度都是「日」。可以加：本週累計成交值排行、本月進榜次數最多、本月新進榜清單。

---

## 明確不做的事

- 不加登入、不加後端 API、不加資料庫——沒有伺服器的靜態架構是這個專案的核心優點
- 不做盤中即時報價（資料源是收盤行情）
- 不提供買賣訊號或投資建議
