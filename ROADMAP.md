# Roadmap

目前這個網站是「成交值排行榜」——回答的是「今天誰在榜上、誰進誰出、名次怎麼走」。
方向是往「**資金流向儀表板**」走：從「誰在榜上」到「錢往哪裡去」。

這份文件把想做的事整理成優先級。標記 ✅ 的已經做完，規格保留下來當作實作紀錄；
其餘照優先級往下走。

## 優先級總表

| | 項目 | 使用者的問題 | 工程量 | 需要新資料源 |
|---|---|---|---|---|
| ✅ | [資料新鮮度警示](#p0資料新鮮度警示) | 我看到的是今天的盤嗎？ | XS | 否 |
| ✅ | [大盤走勢與集中度](#p1大盤走勢與集中度) | 市場熱不熱？資金在集中還是擴散？ | S | 否 |
| ✅ | [個股全期間統計](#p1個股全期間統計) | 這檔是老面孔還是第一次來？ | S–M | 否 |
| ✅ | [產業別與資金流向](#p2產業別與資金流向) | 今天哪個族群在動？ | M–L | 是（TWSE 公司基本資料） |
| ✅ | [題材族群](#p2題材族群) | 在動的是 CCL 還是被動元件？ | M | 否（人工維護對照表） |
| ✅ | [連動美股對照](#連動美股對照) | 今晚要盯的是哪幾檔美股？ | S | 否（人工維護對照表） |
| ✅ | [美股連動頁](#p2美股連動獨立頁面) | 那些標註，資料同意嗎？漲跌幅多少？ | M | 是（Yahoo 美股日線） |
| ✅ | [導覽分層](#p2導覽分層) | 十五個分頁擠在一列，找不到我要的那一頁 | S | 否 |
| ✅ | [篩選與排序](#p3篩選與排序) | 為什麼榜上都是 ETF？ | S | 否 |
| ✅ | [自選股與客製推播](#p3自選股與客製推播) | 我在意的那幾檔有動靜嗎？ | M | 否 |
| ✅ | [上櫃（TPEx）涵蓋](#p4上櫃tpex涵蓋) | 為什麼搜不到某檔櫃買股？ | M | 是（TPEx API） |
| ✅ | [爆量分頁](#p3爆量分頁) | 今天哪幾檔是量放大又收紅的？ | S–M | 否（既有來源多抓兩個欄位） |
| ✅ | [均線分頁](#p3均線分頁) | 誰剛站上或跌破均線？ | M | 否（既有來源多存全市場收盤價） |
| ✅ | [MACD 分頁](#p3macd-分頁) | 誰剛交叉？誰明天就會交叉？ | M | 否（與均線同一份收盤價） |
| ✅ | [法人買賣超](#p2法人買賣超外資與投信同不同邊) | 今天外資與投信同時買了誰？ | M | 是（三大法人買賣超日報） |
| ✅ | [外資連買／連賣榜](#p2外資連買連賣榜) | 外資一路買下去的是哪幾檔、買了幾天？ | S–M | 否（既有的法人每日檔） |
| ✅ | [完整籌碼排行（含逆勢買超）](#p2完整籌碼排行含逆勢買超) | 今天誰買最多？其中誰是在跌的時候買的？ | M | 否（既有的法人每日檔 + close/） |
| ✅ | [法人跨日累計](#p3法人跨日累計近-5-日與-20-日) | 這幾天下來，法人到底買了多少？ | S–M | 否（既有的法人每日檔） |
| ✅ | [力道標](#p3力道標今日買賣超是平常的幾倍) | 今天這筆買超，對這一檔來說算大嗎？ | S | 否（另出一份 insti/base/） |
| ✅ | [法人均價](#p3法人均價買進與賣出價位) | 法人這一批大概買在什麼價位？ | M | 否（但精度有結構性限制） |
| ✅ | [個股的法人歷史](#p3個股的法人歷史) | 這一檔過去幾週法人怎麼進出的？（含走勢圖均價線） | M | 否 |
| ✅ | [具名籌碼異動](#p3具名籌碼異動土洋同買對作) | 今天有哪幾檔土洋同買、哪幾檔在對作？ | S–M | 否 |
| ✅ | [籌碼異動監控](#p4籌碼異動監控自選股不限檔數) | 我在意的那幾檔籌碼有動靜嗎？ | S | 否 |
| P4 | [籌碼雷達](#p4籌碼雷達多條件排行與歷史回看) | 同時符合好幾個籌碼條件的是哪幾檔？ | L | 否 |
| P4 | [進榜後表現統計](#p4進榜後表現統計) | 新進榜之後歷史上通常怎麼走？ | M | 否 |
| P5 | [匯出與分享](#p5匯出與分享) | 我想把這份榜單帶走 | S | 否 |
| P5 | [週／月維度排行](#p5週月維度排行) | 這週累計誰最熱？這個月誰進榜最多次？ | M | 否 |

---

## P0：資料新鮮度警示

> ✅ 已完成。`paintChrome()` 在最新資料距今超過 4 個日曆日時，於頂部顯示紅底提醒。

**使用者的問題**：我現在看到的是今天的盤嗎？

現況：前端 `paintChrome()`（`docs/app.js`）只是安靜地把 `index.json` 的 `latest` 塞進日期下拉選單。
排程若停擺，畫面看起來完全正常，使用者會以為看到的是當天的資料。這是唯一一個「不做會給出錯誤資訊」的項目。

規格：

- meta 列在 `latest` 距今超過 1 個台北交易日時顯示醒目提示：`資料為 8/17，已 N 個交易日未更新`
- 判斷寫在 `paintChrome()` 裡，用 `index.updated`／`index.latest` 與瀏覽器當下時間比對，不需要後端
- 週末與國定假日會誤判成「未更新」，門檻抓寬一點（例如超過 3 個日曆日才提示），寧可少報不要多報

順帶要查的維運問題：`.github/workflows/daily.yml` 的 `cron: "30 8 * * 1-5"` 是否真的有在跑。
GitHub 會自動停用「60 天沒有任何活動」的 repo 上的排程工作流程，停用時不會有通知。

---

## P1：大盤走勢與集中度

> ✅ 已完成。`index.json` 新增 `top200Values`／`top10Values`，前端多了「大盤」分頁。
> 實測集中度確實有訊息量：前 10 大佔比在 21.9%～47.2% 之間擺盪。

**使用者的問題**：市場整體是熱是冷？錢是集中在少數幾檔，還是擴散到很多檔？

`index.json` 已經存了 485 天的 `marketValues`（每日大盤成交值），但前端只拿它顯示 meta 列上的一個數字。
畫成走勢就是市場溫度計；再加上「前 200 大佔大盤的比重」，就得到一條比任何個股都有訊息量的集中度曲線。

### 資料面（改動很小）

`build_history.py` 的主迴圈裡 `payload["stocks"]` 當下就在手上，順手加總即可：

```python
top200 = sum(s["value"] for s in payload["stocks"] if s["rank"] <= twse.STREAK_RANK)
top10  = sum(s["value"] for s in payload["stocks"] if s["rank"] <= 10)
top200_values.append(round(top200 / 1e8, 2))
top10_values.append(round(top10 / 1e8, 2))
```

寫進 `index.json` 兩個與 `dates` 平行的新陣列 `top200Values` / `top10Values`。
485 個交易日大約多 8 KB；`index.json` 是前端唯一用 `cache: 'reload'` 抓的檔案，這個量可以接受。

`build_history.py` 本來就每次由 `daily/` 全量重算，加完欄位跑一次就會補齊全部歷史，不需要重新回補。

### 前端：新增「大盤」分頁 `#/market`

- 路由註冊在 `docs/index.html` 的 tab 列與 `docs/app.js` 的 `render()`
- 三張圖，沿用既有的 `drawLine()` 與 `pills('span', ...)`（60／120／全部）：
  1. 大盤成交值走勢（億元）
  2. 前 200 大成交值合計走勢
  3. 集中度走勢——`top200Values[i] / marketValues[i]`、`top10Values[i] / marketValues[i]` 兩條線
- 頂部沿用 `.stat-grid`：當日大盤成交值、對比前一日 ±%、前 200 佔比、前 10 佔比

`drawLine()` 目前只吃單一 dataset，集中度雙線圖需要把它小改成接受 datasets 陣列
（比畫成兩張圖好，P2 的族群比較也會用到）。

---

## P1：個股全期間統計

> ✅ 已完成。個股頁拆成「近 N 個交易日」與「全期間紀錄」兩張卡。

**使用者的問題**：這檔是天天在榜的老面孔，還是今天第一次來？以前來過幾次？最好曾經到第幾名？

現況缺口：個股頁 `renderStock()`（`docs/app.js`）的「期間最佳」與「進前 200 天數」
**只算目前選定區間（預設 120 日）內**的資料——因為統計是在 `slice` 出 labels 之後才做的。
使用者會以為那是歷史紀錄，其實只是最近幾個月。

### 資料面：完全不用改 Python

`seriesFor(code)` 已經把所有年度的 `history/{year}.json` 合併成完整的 `byDate: Map<date, {rank, value}>`，
全期間資料本來就在記憶體裡，只是被區間切掉了。單檔最多 485 個點，前端計算成本可忽略。

### 新增指標

依 `state.index.dates` 的順序掃過 `byDate`，以 `TOP = 200` 為進榜門檻：

| 指標 | 算法 |
|---|---|
| 總進榜天數 | `byDate` 中 `rank <= 200` 的天數 ／ 全部交易日數 |
| 進榜次數（波段數） | `rank <= 200` 的連續區段數量 |
| 歷史最佳名次 | `min(rank)` 與發生日期 |
| 最長連續天數 | 最長區段長度與起訖日 |
| 上次掉出榜 | 最後一個已結束區段的下一個交易日 |
| 目前狀態 | 在榜中（連 N 天）／已掉榜 M 天 |

兩個容易踩到的邊界：

- `byDate` 含 rank 201–300 的日子（daily 存 300 名是為了判斷進出榜），
  統計「進榜」時必須過濾 `rank <= 200`，不能只看那天有沒有值
- 連續區段的相鄰性要用 `index.dates` 的**索引**判斷，不能用日曆日相減（週末與休市日會斷）
- 若某段連續紀錄一路連到 `index.dates[0]`，沿用既有 `streakLabel()` 的 `+` 標示慣例

### UI

個股頁的統計格拆成兩區：「本區間」（現有六格，標題明確寫出是哪段區間）
與「全期間」（新的六格）。區間 pills 只影響前者與圖表。

---

## P2：產業別與資金流向

> ✅ 已完成，但有一處與原規格不同：**沒有產出 `docs/data/sector/{year}.json`**。
> 族群頁只需要當日與比較基準日的資料，而這兩份 daily 檔前端本來就會載入，
> 直接在前端 join `industry.json` 就夠了，不必多存一份約 1 MB 的衍生資料，
> 也避免產業歸類規則在 Python 與 JS 兩邊各寫一次。
> 之後若要做「產業成交值的長期走勢圖」，再回來補這份檔案。

**使用者的問題**：今天是 AI 伺服器在動還是航運在動？

這是看排行榜的人腦中真正的問題，而不是「第 37 名是誰」。目前資料裡完全沒有產業欄位，
這是最大的資料缺口，也是 P0–P2 裡唯一需要接新資料源的項目。

### 新資料源

端點：`https://openapi.twse.com.tw/v1/opendata/t187ap03_L`（上市公司基本資料，1095 筆）。

實測結果：`產業別` 欄位是**數字代碼**（台積電是 `"24"`）而不是中文，所以
`scripts/fetch_industry.py` 內建一份代碼→中文對照表（逐一比對過樣本公司），
對不到的代碼會歸為「其他」並在執行時印出提醒。目前 33 個產業、1089 檔，
最新一個交易日的前 200 名 100% 都對得到。

`scripts/fetch_industry.py` → `docs/data/industry.json`：

```json
{"updated":"2026-08-29T16:30:00+08:00","map":{"2330":"半導體業","2317":"電腦及週邊設備業"}}
```

- 沿用 `twse.fetch_json()` 與 `twse.write_json()`，不需要新的抓取邏輯
- ETF／ETN（`00` 開頭）不在公司清單裡 → 統一歸類為 `ETF`
- 4 碼＋英文字母的特別股（如 `2887Y`）不在清單裡 → fallback 到去掉字母的母公司代碼
- 對不到的一律歸 `其他`，並在 log 印出數量，資料源改格式時才看得出來
- 內容變動極慢，`daily.yml` 每天跑一次即可；沿用既有「內容沒變就不重寫」的作法，git diff 不會有雜訊

### 產業欄位不寫進 `daily/*.json`

485 個 daily 檔全部重寫會產生 20 MB 的 git diff，而且產業別是「現況」屬性、不是當日快照。
改成前端載入 `industry.json` 之後 join，一次 HTTP 請求就解決。

聚合直接在前端做（見章節開頭的說明）。`industry.json` 抓不到時，
整站當作沒有產業資訊：族群頁顯示提示、排行頁不出現產業標籤與篩選。

⚠️ 要在 UI 註明：產業別是用「今日的分類」回溯套用到歷史資料（`build_history.py` 每次全量重算），
公司改變產業歸屬時，歷史數字會跟著變。

### 前端：新增「族群」分頁 `#/sector`

- 當日各產業一列：榜上檔數、成交值合計（億）、佔前 200 大比重、對比前 1／5／20 日的變化
  （沿用既有的 `BASELINES` pills）
- 點一列展開該產業當日在榜的個股（沿用 `stockRow()`）
- 排行頁每一列加上產業標籤，並支援「只看某產業」

### 後續：族群頁改成資金流向看板

> ✅ 已完成。原本每列只有「成交值 + 變化百分比」，回答不了「錢往哪一族跑」。

原因是單看百分比變化會被大盤縮放蓋掉：整體縮量 13% 的日子裡，幾乎每一族都是負的，
看起來像全面失血，實際上錢只是從其中幾族轉到另外幾族。所以每列改成三個一起看的數字：

| 數字 | 意義 | 侷限 |
|---|---|---|
| 成交值增減（億） | 直覺的「錢變多還變少」 | 大盤縮量時全部都會是負的 |
| 佔比位移（pp） | 抽掉大盤縮放後，錢從哪一族轉到哪一族 | 是相對量，全市場一起放大時看不出來 |
| 成交值加權漲跌幅 | 這些錢把價格推上去還是砍下來 | 只是當日的價格反應，不是籌碼歸屬 |

實測 2026-08-17 vs 前一日（全部範圍）：榜上總額 -13.7%，14 族流出、10 族流入，
但光電業 +176 億（+47.3%，加權 +6.07%）、航運業 +151 億——縮量的日子裡錢明確往這兩族集中。
反向的例子是通信網路業：成交值 -15 億、佔比卻 +1.12pp，只是比大盤少縮而已。

其餘實作重點：

- 頂部多一張「榜上資金總量」卡：前 200 大成交值、整體增減、全榜加權漲跌、流入／流出族群數。
  沒有這個分母，個別族群的負值會被誤讀成該族失寵
- 排序 pills：依資金增減／依佔比位移／依成交值（`state.sectorSort`，預設資金增減）
- 每列一條由中線往兩側延伸的橫條，長度對比當日最大流量；紅右綠左，與台股漲跌色一致
- **基準日在榜、當日整族退出前 200 大的族群要單獨列出**（`gone`），否則最徹底的資金流出
  反而完全看不見。這類列沒有當日成分股可展開，畫成一般的 `.row`
- 加權漲跌幅用成交值當權重：族群的資金流向本來就由成交值大的那幾檔決定
- 已知的天花板：成交值分不出買方賣方，這裡看到的是「資金熱度的轉移」，
  不是三大法人那種真正的淨買賣超。後者已由
  [法人分頁](#p2法人買賣超外資與投信同不同邊)補上，走的是另一個資料源（三大法人買賣超日報）

### 後續：「流向」分頁，把同一批數字畫成圖

> ✅ 已完成。族群頁是一張表，適合查數字；流向頁是兩張圖，適合一眼看出形狀。

- **資金地圖**：squarified treemap，面積＝當日成交值、顏色＝相對基準日的增減。
  顏色吃的是增減**幅度**不是金額——用金額上色會被半導體那種量體整片洗掉，
  小族群翻倍就看不見了
- **量價四象限**：橫軸成交值增減（量）、縱軸成交值加權漲跌（價）、
  泡泡大小成交值、顏色佔比位移，四個維度都是同一天的同一批數字。
  右上角量價齊揚是資金進駐，右下角爆量下殺是出貨
- 座標軸上限取第 85 百分位而不是最大值：冷門族群成交值翻三倍是常有的事，
  拿它當上限會把其餘二十幾族擠成中間一坨。超出範圍的貼在邊緣，真實數字留在 tooltip
- 四象限的名字不設上限，塞得下就標。初版只標前六大又碰到就放棄，實際上只印得出四個名字，
  右半邊一整片空地白白空著——而離群的小族群正是最想知道名字的那一種。改成每顆泡泡
  依序試上、下、右、左四個位置，不壓到別人的字也不壓到任何泡泡就落腳；成交值大的先搶。
  前六大例外：它們擠在同一坨裡永遠找不到乾淨的位置，但少了名字整張圖就沒有錨點，
  所以放寬成「只要不壓到別人的字」，靠 `paint-order` 描一圈底色當白邊讀出來
- 四象限的裝飾一律只做「暗示」不做「宣告」：右上與左下各一塊從角落淡出的暈色
  （14% 起、到中線歸零），零軸改成虛線退到泡泡後面，左邊補一個 `0` 讓中線有名字。
  暈色刻意壓得很淡——泡泡的紅綠講的是佔比位移，跟象限的紅綠不是同一件事，
  底色搶戲就會被誤讀成同一個維度
- 泡泡照成交值由大到小依序長出來（每顆差 22ms），一次全部冒出來看不出誰先誰後；
  `prefers-reduced-motion` 時整個關掉
- 四象限的底盤做成一塊儀表：面板上下漸層、鋪一層八等分的細網格、四角切出瞄準框，
  零軸末端加方向箭頭、原點加一個小圓環。切八等分是為了讓正中間那條剛好落在零軸上，
  格線本身就是「離零多遠」的刻度，不必再標一排數字
- 泡泡改畫三層：主體、外圈虛線瞄準環、中心實心點。中心點不只是裝飾——
  二十幾顆疊成一坨時，圓心在哪一眼就看得出來。環與點只給大顆的，
  小泡泡本來就只有幾個 px，再加東西只會變成一團髒點
- 面板的漸層與網格用 `fill="url(#…)"` 寫在 SVG 屬性上，**不能寫在外部 CSS**：
  外部樣式表裡的 `url(#id)` 是相對樣式表解析的，會變成 `style.css#id` 而找不到
- 深淺兩色各給一組面板變數（`--q-panel-a/b`、`--q-edge`、`--q-mesh`、`--q-halo`）。
  淺色是安靜的藍圖感；深色把面板壓得比卡片還暗、泡泡的輝光加重，就成了霓虹。
  網格線與面板的對比壓在 1.4 上下——再淡就只剩「好像有東西」，再濃就跟資料搶眼睛
- 兩張圖都用純 DOM／SVG 手刻，**不依賴 Chart.js**。個股頁那兩張線圖離線時會掛掉，
  這一頁不該重蹈覆轍；treemap 若要用 Chart.js 還得多接一個 plugin
- **squarify 一定要在盒子真實的長寬裡算**。初版在 100×100 的百分比空間裡算完再拉開，
  正方形裡排得漂漂亮亮的一組方塊，拉成 3:1 之後每一塊都跟著橫向拉長三倍——
  手機的地圖接近正方形所以看不出來，桌機滿版就整片扁掉（實測中位長寬比從 1.28 變成 4 以上）。
  改成先量盒子再排版，算完才換算成百分比；尺寸一變就整張重排
- 尺寸有時候分兩步到位（媒體查詢換了長寬比、手機轉向的中間狀態），只認第一次量到的
  會定在中間那個尺寸上，所以下一幀再確認一次
- 地圖的字是量出來的不是猜的：塊用百分比定位，但「放不放得下字」是絕對尺寸的問題，
  所以等畫出來以後量 `getBoundingClientRect()` 再決定字級、要不要折兩行、要不要收起來。
  用 `ResizeObserver` 盯著地圖本身而不是 window 的 resize——`observe()` 一掛上就會先送一次，
  初次量測與後續變化同一條路徑；背景分頁不跑 observer，所以掛上前還是自己先量一次
- 估字寬時中日韓算一個字寬、英數與半形符號算 0.55。全照中文寬度算的話，
  「-244 億」會被當成比實際寬一倍，窄格子上的金額就會被誤判成放不下
- 折幾行不預設，由大到小試字級、取塞得下的最大的那個（`fitName()`）。同一個名字折一行
  還是三行，能用的字級差很多，哪一種最好完全看塊的形狀：「文化創意業」在 43x38 的塊裡
  擠一行只能用 9px 還被切，折兩行可以用 10px；「電器電纜」在 25x47 這種高瘦的塊裡，
  一個字一行直著排反而讀得到。**行數一定要用「一行塞得下幾個字」回推**——直接拿寬度
  除以字數會算出小數個字，CSS 實際折出來的行數比算的多，多的那行就被 line-clamp 切掉
- 名字被切成「軍工 · 航太…」還讀得懂，金額被切成「+120…」卻會被誤讀成別的數字，
  所以金額只在最小字級也塞得下時才顯示，塞不下就整個不出現
- **這一頁是全站唯一有桌機版型的地方**：地圖的塊夠不夠大看的是「面積」，手機寬度窄，
  就得靠高度把面積補回來（手機 1:1.1 直的、桌機 1:0.66 寬的）
- 四象限跟地圖一樣，**畫在盒子真實的像素裡**（`quadGeom()`，1 單位＝1px），不再用固定
  360×300 的 viewBox 讓 CSS 等比拉大：等比拉大時字、泡泡、瞄準框、線寬會一起脹成 1.7 倍，
  只能限寬 540px 擺在中間，右邊一整片螢幕空著。改成 1:1 之後多出來的寬度全變成散開的空間，
  同一批資料標得出名字的族群從 13 個變成 19 個。盒子的長寬比照螢幕換
  （手機 1:0.83、≥560px 1:0.62、寬螢幕 1:0.5），SVG 絕對定位填滿它——
  用正常流的話 SVG 會反過來把盒子撐高，ResizeObserver 量到自己畫出來的高度就無止境地重畫
- 畫布變大時字、留白、泡泡放大的速度不一樣：字幾乎不動（10px 起跳、14px 封頂，
  再大就變海報），留白跟著字走（它就是為了放得下刻度），泡泡照面積開根號放大到 2.4 倍
  （泡泡是用面積在講話，畫布大了還維持手機尺寸會像沒有資料）。「畫布有多大」一律用
  面積開根號算，不能用寬或高——桌機的圖又寬又扁，拿高度算會判成小畫布，
  拿寬度算又會在 800px 的平板上就頂到最大
- 寬螢幕（≥ 900px）下這一頁滿版（`.view.wide`，由 `render()` 依路由掛上），
  其他分頁維持 720px：那些都是清單，拉寬只會把每一列扯開，地圖與四象限則是給多少用多少。
  兩張圖都加 `max-height: 72vh`，螢幕再寬也不會高到要捲兩次才看得完；
  說明文字限 92ch，不然拉到 1900px 寬沒人讀得下去
- 四象限的標籤先搶先贏：泡泡照成交值由大到小畫，會疊到已放好的標籤就不標——
  兩個名字疊在一起等於兩個都讀不到
- 題材族群模式下一檔可屬多個族群，地圖的總面積會大於榜上總額（重疊的部分兩邊都算）。
  比的是彼此的相對大小，不是切分同一塊餅——這點要在 note 裡講明白

誠實標示：這是官方產業分類（「半導體業」「電子零組件業」），
不等於市場口中的「AI 伺服器」「重電」等題材族群。

## P2：題材族群

> ✅ 已完成。族群頁上方多了「官方產業／題材族群」切換，兩種分類軸**並存**而不是二選一。
> 對照表在 `docs/data/themes.json`（人工維護，21 個大族群 / 71 個子族群 / 413 檔）。

**使用者的問題**：今天在動的是「電子零組件業」，還是「CCL」跟「被動元件」？

官方分類的問題不是不準，是顆粒度不對：台光電（CCL）、國巨（被動元件）、川湖（機構件）
同屬「電子零組件業」，在同一格裡互相抵消，看不出資金往哪一段跑。
題材族群補的就是這一層——但它換不掉官方分類，因為官方分類自動更新、永遠不漏檔。

### 資料：`docs/data/themes.json`

`{ groups: [{ name, subs: [{ name, codes: [] }] }] }`，兩層。**這是人工維護的資料檔**
（另一份是後來加的 `us_link.json`，見「連動美股對照」），放在 `docs/data/` 下但不由任何腳本產生，
`daily.yml` 也不會動它。

不寫進檔案、由前端自動處理的兩類：

- ETF 依 `industryOf()` 歸入「ETF · 指數商品」——新掛牌的 ETF 不必手動補
- 名單外的個股歸「未分類」，並固定排在最後（它是名單的缺口，不是族群）

### 前端

- `byTheme()` 與 `bySector()` 回傳同一種形狀，`renderSector()` 只換 `group` 函式
- **一檔可屬多個族群**，佔比的分母不能拿各族群加總（會把重複計算算進去），
  一律取「榜上成交值總額」，每檔只算一次。官方產業模式下兩者本來就相等
- 族群頁在 note 裡誠實標示名單的維護日期與未歸類檔數
- 排行頁篩選下拉加一個 `<optgroup>`，大族群與子族群都能選（值以 `T:` 前綴區分）
- 選到的分類軸存在 localStorage（`stocktracker.grouping`）

### 歸類的可信度

初版有六檔是憑印象放的，事後逐一查證公司官網與公開資料，**五檔是錯的**：

| 代號 | 初版 | 實際業務 | 更正為 |
|---|---|---|---|
| 6727 亞泰金屬 | 機殼機構件 | CCL 含浸設備（R2R），PCB 基材製程設備占營收 91% | CCL／CCL 製程設備 |
| 7795 長廣 | 散熱 | IC 載板真空壓合機 | PCB 載板／PCB 設備耗材 |
| 2493 揚博 | 被動元件 | PCB／LCD／半導體製程檢測設備＋藥水（設備 59%、耗材 38%） | PCB 載板／PCB 設備耗材 |
| 3441 聯一光 | 光收發模組 | 光學玻璃、透鏡／稜鏡毛胚 | 光學面板／鏡頭光學元件 |
| 2486 一詮 | 面板背光 | 散熱（均熱片／Metal TIM）已占營收 43–46%，超越 LED 導線架 | 加掛 AI 伺服器／散熱 |
| 7610 聯友金屬 | 資源循環 | 鎢／鈷回收與高純度化合物，供半導體與精密加工 | 傳產／金屬回收稀有金屬 |

教訓：**中小型股不能憑股名或官方產業別猜**，尤其是設備廠——名字裡有「金屬」的是 CCL 設備龍頭，
歸在「電子零組件業」的是 PCB 製程設備商。要擴充名單就得一檔一檔查官網或年報的營收比重。
另一個提醒是分類會過期：一詮的主力已經從 LED 導線架換成 AI 散熱，名單要跟著營收結構走。

### 歸類的方法

上櫃榜尾那 107 檔是用**公開資訊觀測站的「主要經營業務」**批次查出來的
（`ajax_t05st03`，以 `co_id` 逐檔 POST）。這份欄位是公司自填的法定揭露，
比任何財經網站的分類都可靠，而且可以程式化取得——107 檔約兩分鐘跑完。

其中 12 檔的揭露太籠統（「電子零組件製造業」這種），再逐檔查營收比重補齊。
最後 3 檔（5392 能率、1336 台翰、6199 天品）業務組合太雜，任何族群都不貼切，
**刻意留在「未分類」**——名單的可信度比覆蓋率重要。

要再擴充名單就照這個流程走：先批次抓 MOPS 主要經營業務，再對揭露籠統的補查營收比重。

### 已知缺口

最新交易日的三種範圍幾乎全覆蓋。但名單是照「今天的榜」補的，**回看較早的日期仍會出現「未分類」**：
485 天裡曾進過「全部」榜前 200 大、卻不在名單上的還有 582 檔，其中上榜 20 天以上的有 170 檔
（上榜最久的是 6781 AES-KY，372 天）。要補就從上榜天數高的往下做。

### 連動美股對照

> ✅ 已完成。題材族群模式下展開一族，第一行就是這一族在美股的對照。
> 對照表在 `docs/data/us_link.json`（人工維護，21 大族群 / 69 個子族群 / 175 檔美股）。

**使用者的問題**：今晚要盯的是哪幾檔美股？隔天早上跳空的是哪一族？

台股電子股的隔日開盤，很大一部分在前一晚的美股就決定了。但「看美股」講得太籠統——
NVDA 漲 5% 不代表整片電子都會跟，得知道這一族真正對照的是哪幾檔。

`{ groups: [{ name, why, us: [{ t, n, s }], subs: [{ name, us }] }] }`，
**靠族群名稱與 `themes.json` 相接**（兩份檔案各自維護，名字對不上就當作沒有對照，
不會讓族群頁掛掉）。另有 `benchmarks`：TSM ADR、費半、那斯達克、十年期殖利率、美元指數，
那是大盤層級的參考，不屬於任何一族。

`s` 是連動強度，只有三級——這是整份檔案最重要的欄位：

| | 意思 | 例子 |
|---|---|---|
| ★★★ | 同一條供應鏈：客戶的財報或資本支出直接決定訂單 | 2345 智邦 ↔ ANET、記憶體 ↔ MU、6669 緯穎 ↔ SMCI |
| ★★☆ | 景氣或報價同步：同一個循環，中間隔著報價與匯率 | 功率元件 ↔ ON／TXN、鋼鐵 ↔ NUE、製鞋 ↔ NKE |
| ★☆☆ | 題材情緒連動：跳空之後常收斂，不宜追價 | 軍工 ↔ LMT、電信商 ↔ T／VZ、生技 ↔ LLY |

**沒有對照就讓它空著**：71 個子族群裡有 2 個（CCL 製程設備、陶瓷／電子基板材料）
在美股找不到對得上的同業（同業在日本與台灣），填一檔勉強的比空著更糟。
被動元件整族也只給到 ★★☆——真正的同業是村田與 TDK，不在美股。

**誠實標示**：這是觀察的起點而不是訊號。真正的驅動常是報價指數（SCFI、BDI、DXI、合約價）
與匯率，美股只是同一個訊號比台股早幾個小時反應；族群頁的 note 要把這句話寫出來。

---

## P2：美股連動（獨立頁面）

> ✅ 已完成。`docs/us.html`＋`docs/us.js`，**不是排行榜那支 SPA 的分頁**，只共用 style.css。
> 入口放在排行榜的頂部工具列（不是 tab 列）：只放頁尾等於沒人找得到——排行頁的頁尾在兩百列之後。
> 資料由 `scripts/fetch_us.py`（抓 Yahoo 日線）與 `scripts/build_us.py`（算相關性）產生，
> 兩支都是手動執行，沒有接進 `daily.yml`。

**使用者的問題**：那些人工標的星等，資料同意嗎？還有，昨晚那幾檔美股到底漲跌多少？

`us_link.json` 的星等是憑供應鏈關係給的，講得出道理不代表盤面真的這樣走。
這一頁做的就是把標註拿去對資料驗證：**真的算一次相關係數，用百分比講出來**。

### 對齊方式：美股 T 日 → 台股 T+1 日

美股收盤（台灣時間清晨）在台股開盤之前，因果方向只有一個。
把同一個日曆日擺在一起是錯的——那等於拿台股當天的收盤，去對美股還沒開盤的行情。
台股連假時同一個台股交易日會對上好幾個美股日，只留**最近的那一晚**。

### 兩個相關係數，都要給

| | 算法 | 為什麼需要 |
|---|---|---|
| 原始相關 r | 兩邊日報酬的皮爾森相關係數 | 直覺，但會被「整個市場一起動」灌水 |
| 超額相關 x | 兩邊各自先扣掉自己市場的大盤（台股扣 ^TWII、美股扣 ^IXIC）再算 | 抽掉共同的市場因子，剩下的才是這一對自己的關係 |

只給 r 會騙人：台股電子股彼此本來就有五、六成的相關，對上任何一檔美股大型股都不難看。
**但 x 對權值股要打折看**：台積電自己就佔加權指數約三成，扣大盤等於把它自己扣掉一大半，
NVDA × 台積電 的 x60 因此是負的——那是算法的限制，不是它跟美股脫鉤。這句話要寫在頁面上。

### 資料範圍的硬限制

台股價格取自 `kline/`，那是由 `close/` 轉置出來的，只從 2026-03-16 開始（全市場四價是後來才存的）。
**所以相關性最長就是半年**，觀察窗給到 120 日就是全部，再長沒有資料——這不是參數可以調的。

### 配對與強度的兩個坑

1. **★★★ 的配對一律列出**，不管相關性排第幾名：那正是要驗證的東西。
   NVDA 橫跨六族、配對池 179 檔，不這樣做的話台積電會被排名截掉，看起來像漏了。
2. **族群層級的標註不能無條件整族套下去**。ANET 掛在「網通 · 交換器」是 ★★★，
   指的是交換器那一段，不是同一族底下的電信商——初版就這樣把 ANET × 台灣大標成了 ★★★，
   而它的實際相關是 **−34%**。規則改成：子族群自己列了美股就代表它講得清楚，
   族群層級那些沒被它列到的標的對它**降一級**；子族群沒列才整族照抄。

### 抓資料時發現的事

對照表初版有五檔美股 Yahoo 回 404，全是併購與改名造成的下市：
JNPR（被 HPE 併）、X 美國鋼鐵（被日本製鐵併）、PSTG、IGT（改名 Brightstar Lottery，
新代號 **BRSL**）、LNW（主掛牌移到 ASX，美股只剩 OTC 的 **LNWO**）。
**人工維護的對照表會因為公司重組而爛掉**，不是寫完就不用管——
`fetch_us.py` 抓不到的代號會一檔一檔印出來，那就是該回頭修對照表的訊號。

### 幾個第一版就看得到的結果

| 配對 | 標註 | r60 | x60 |
|---|---|---|---|
| TSM × 2330 台積電 | ★★★ | +55% | — |
| COHR × 3450 聯鈞 | ★★★ | +56% | — |
| ANET × 2345 智邦 | ★★★ | +45% | +31% |
| MU × 2408 南亞科 | ★★★ | +35% | +20% |
| SMCI × 6669 緯穎 | ★★★ | **+12%** | +4% |
| AAPL × 2374 佳能 | ★★★ | **−12%** | — |

最後兩列就是這一頁存在的理由：供應鏈上講得通，日報酬上對不起來。

### 沒做的事

- **沒有接進每日排程**：177 檔要抓兩分多鐘，Yahoo 在 CI 上的限流行為還沒實測過。
- **沒有拿美股去掃全市場台股**：配對只走人工對照表畫出來的範圍。
  真要做「發現式」的相關性排行，得先處理「整個市場一起動」造成的假訊號。
- **沒有畫圖**：這一頁是表，散點圖與滾動相關留給以後。

---

## P2：法人買賣超（外資與投信同不同邊）

> ✅ 已完成。成交值分不出買方賣方，這一頁補的是真正的淨買賣超。
>
> 別名：市場上把這張主榜叫「**土洋同買／同賣**」。對作（外資買、投信賣）還沒做，
> 見 [具名籌碼異動](#p3具名籌碼異動土洋同買對作)。

**主榜只有一張：外資投信同買／同賣。** 這一頁不做「外資買超排行」——
那張榜前幾名幾乎天天是同一批權值股，看久了沒有資訊；而且只有一邊在動很難解讀
（外資買超十億可能只是某檔 ETF 在建倉，投信賣超三億可能只是基金在應付贖回）。
（**後來補了**：單邊排行配上「逆勢」那一刀之後是有資訊的，所以它自成一頁，
見 [完整籌碼排行](#p2完整籌碼排行含逆勢買超)。）
外資動的是全球資金的配置、常常是被動的；投信動的是國內主動型基金，經理人得對每一筆
負責。兩種完全不同的決策流程在同一天指到同一檔，才是這一頁要挑出來的東西。

**門檻是「兩邊各自」都要達到，不是合計**（0.3／0.5／1／3 億可選，預設 0.5）。
用合計的話，外資買超十億配上投信賣超九億也會進榜，方向卻是相反的。

### 新資料源：兩支都能指定日期

| | 上市 | 上櫃 |
|---|---|---|
| 個股買賣超（股數） | `fund/T86` | `insti/dailyTrade?sect=EW` |
| 全市場合計（金額） | `fund/BFI82U` | `insti/summary` |

兩支個股來源都能指定任一交易日，所以不像成交值排行那樣要分「當日」與「歷史回補」
兩支腳本 —— `fetch_institutions.py --days N` 一支就夠，已存在的日期自動跳過。

### 金額是估算的，全市場合計不是

**官方的個股資料從頭到尾只有股數。** 金額只出到全市場的彙總表，沒有分到個股，
所以個股的金額一律是「買賣超股數 × 當日收盤價」。拿全市場合計去對官方彙總表
（22 個交易日、兩個市場分開算）：外資的相對誤差中位數約 5%、金額差距中位數 2.8 億。
量級與方向可靠，但門檻邊緣的個股會因為這幾個百分點進出榜。

所以每日檔裡 `total`（官方金額）與 `est`（個股估算的合計）兩種都存 ——
兩者並存才講得出「這份估算差多少」，畫面上最顯眼的那六格取的是官方的那一份。

### 其餘實作重點

- 欄位位置用**數字之間的關係**驗，不是只認欄名：上櫃那張表的 24 欄裡除了前兩欄全叫
  「買進／賣出／買賣超股數」，欄名認不出誰是誰。驗每一組的買 − 賣 = 買賣超、
  合計組 = 子組相加、最後一欄 = 三邊相加。讀錯欄位不會報錯、只會算出安靜的錯誤答案
- 收盤價讀 `docs/data/close/`（`backfill.py` 已經寫好的那一份），不重抓全市場行情；
  真的還沒有那一天的檔案才現抓一次。排程順序因此固定在 `backfill.py` 之後
- 兩個市場只有一邊有資料時**整天不寫檔**：只寫一半沒有人看得出來，少掉的那個市場
  會被讀成「那邊的法人整天沒動」
- 三邊的估算金額都不到 0.05 億的不存（一天 90 KB -> 43 KB，丟掉的是幾百股的零頭）；
  門檻寫進檔案的 `cut` 欄位，讀的人才知道邊界在哪
- 每一列的金額底下是「**佔成交值**」＝外資與投信合計 ÷ 當日成交值。同一個門檻套在
  大小型股上意義完全不同，這一格才看得出來是零頭還是被吃掉一大塊
- 法人資料涵蓋的交易日比排行短（後來才開始累積），所以要有自己的 `insti/index.json`：
  「那一天還沒抓到」與「檔案掛了」必須在畫面上分得出來

## P2：外資連買／連賣榜

> ✅ 已完成。`#/instirun`（分頁「連買」）。法人頁看的是一天，這一頁看的是**同一個
> 方向撐了幾天**：外資連續買超（或賣超）3 天以上的名單與天數。
>
> 別名：市場上講的「**外資停留**」「外資連續買賣天數」就是這一頁。

單日的買超很容易是別的東西 —— ETF 調整成分、指數換股、避險部位、除息前後的調節，
一天就結束。一筆被動的調整不會連著十天做同一件事，但一個真的在建倉（或減碼）的決定會。
所以天數是主軸，兩張榜都**依天數排序、同天數才比累計金額**：金額大的單日買超在法人頁
看得到，這裡要挑的是持續性。

**兩個門檻。** 天數（3／5／8／12 天以上）決定「多久才算持續」；累計金額
（不限／1／5／20 億）把零頭濾掉 —— 連買天數本身不分大小，一檔債券 ETF 每天被買進
幾十萬元也能連上十幾天，那不是訊號、只是它天天有人申購。兩個都記在 localStorage。

**只算外資。** 投信與自營商的每日資料同樣在法人頁上，但這一頁要回答的是「外資有沒有
一路買下去」；三邊都排出來只會讓人一次看三張榜、一張也讀不完，而且檔案大三倍。

### 沒有新資料源：算在後端，一天一個檔

`insti/daily/` 已經有需要的一切，所以這一頁不多抓任何一支 API。但天數**不能讓前端算**：
一天的檔案 43 KB，「連買 N 天」要回頭看 N 天，最長的那幾段還會連到資料起點 ——
等於把整段歷史下載一次。所以 `build_institutions.py` 一天寫一個
`insti/streak/{日期}.json`（只留 3 天以上的，一天約 13 KB），前端一次只抓一個。
純衍生資料，每次都由 `insti/daily/` 從頭重算，不用重抓。

### 「連續」的三條規則

天數算錯只會偏長，不會偏短，而偏長的天數看起來跟真的一模一樣：

1. **相鄰性看交易日，不是看檔案順序。** 兩個市場只有一邊有資料的日子整天不寫檔，
   所以 `insti/daily/` 中間可能缺一個真正的交易日；照檔案順序往前接的話，缺的那天
   賣超也會照樣算成連買。交易日名單取自 `docs/data/close/twse/`
   —— 與排行頁「連續區段要用交易日索引判斷、不能用日曆日相減」是同一個原則
2. **沒進當天的資料檔就斷。** 缺席代表三邊的估算金額都不到 `cut`（0.05 億），
   外資那天動的是幾萬元的零頭；把它算成買超的一天，等於用資料檔的邊界編造連續性
3. **連到資料起點（或缺口）的標 `trunc`**，畫面上寫成「連 N**+** 天」，
   沿用既有 `streakLabel()` 的慣例

### 每一列都帶期間漲跌

這一頁最容易被誤讀成「跟著買」，而兩邊都常見：外資連賣二十幾天而股價還在漲的有
（2026-09-03 的 8996 高力連賣 22+ 天、累計 -181 億，期間 +15.1%），
連買十幾天而股價沒動的也有。基準取起算日的**前一個交易日**收盤，不是起算日本身
—— 外資是在起算日當天買的，那天的收盤價已經含了這筆買盤推上去的部分。

累計金額逐日以**當日**收盤價換算後相加，不是用最後一天的價格回推：一段連買十幾天的
期間裡股價本來就在動。累計張數則是純粹的股數相加，不受價格影響。

## P2：完整籌碼排行（含逆勢買超）

> ✅ 已完成。`#/instirank`（分頁「買超」）。法人頁問的是「外資與投信有沒有站在
> 同一邊」，這一頁問的是**單邊的大小**：外資／投信／自營／三大法人合計四選一，
> 各自的買超排行與賣超排行，加上兩張**逆勢榜**。

法人頁刻意不做單邊買超排行，理由是「前幾名幾乎天天是同一批權值股，看久了就沒有
資訊」。那個理由是對的，但結論下錯了 —— **該補的不是把那張榜刪掉，是給它第二個軸。**
「今天誰買最多」是看籌碼的人第一個想問的問題，擋掉它只會讓人去別的網站查。

第二個軸是**逆勢**：

| 榜 | 條件 |
|---|---|
| 買超排行 | 這一邊買超 |
| **逆勢買超** | 這一邊買超，**而這一檔當天收黑** |
| 賣超排行 | 這一邊賣超 |
| **逆勢賣超** | 這一邊賣超，**而這一檔當天收紅** |

逆勢那兩張不是另外挑出來的股票，是上面兩張**濾掉順勢的那一半**。

### 為什麼「逆勢」這一刀切得動

順勢的那一半（買超 + 收紅）常常是果不是因：股價自己在漲、買盤跟著追進去，法人的
買超只是那天成交量的一部分，「因為在漲所以有人買」這個最無聊的解釋沒辦法排除。
逆勢的那一半排除得掉 —— 買超是在賣壓裡接的、賣超是在漲勢裡出的。

比例也支持分這一刀：2026-09-03 是大盤下殺的一天（資料檔裡 934 檔算得出漲跌，
201 檔收紅、708 檔收黑），外資買超 303 檔、其中 149 檔是逆勢買超。榜首是聯電
（買超 +27.5 億而收 -0.79%）與聯亞（+18.9 億而收 -8.10%）—— 在單純的買超排行上，
這兩檔跟一堆「漲了所以被追」的混在一起，分不出來。

**但逆勢買超不等於低接。** 被動的指數調整、ETF 換股與避險部位照樣會撞上大盤下殺
的那一天，它們在這張榜上與真的在建倉的錢長得一模一樣。這一刀排除掉的只是最無聊的
那個解釋，不是替使用者做判斷 —— 說明卡裡要寫死這一句。

### 逆勢榜依金額排序，不是依跌幅

第一版想依跌幅排，跌最多而被買最多的擺最上面。不行：跌停板的小型股本來就多，依跌幅
排會把一堆幾百萬的零頭推到最上面，而這張榜要回答的是「**誰在賣壓裡接了最多**」。
所以四張榜一律依估算金額排序，跌幅只當篩選條件。

平盤（漲跌正好是 0）與算不出漲跌的都不算逆勢：逆勢要有「勢」可逆。

### 沒有新資料源，但漲跌要另外算一份檔

`insti/daily/` 已經有三邊的買賣超與當日收盤價，四張榜的排序全部算得出來。少的只有
**漲跌** —— 它要拿前一個交易日的收盤價比，而每日檔只存當天的。

三個選項，選了第三個：

| 做法 | 為什麼不 |
|---|---|
| 前端抓 `close/` 前後兩天 | 四份全市場行情約 240 KB，為了一欄漲跌不划算 |
| 加一欄進 `insti/daily/` | 那份是「從官方抓回來的東西」，加欄位要 bump 版號並把整段歷史重抓一次；而漲跌是本機算得出來的，不該綁在網路來源上 |
| **另寫一份衍生檔** | `insti/chg/{日期}.json`，約 13 KB，由 `build_institutions.py` 每次從 `insti/daily/` 與 `close/` 重算 |

**不用 `daily/{範圍}/*.json` 那份現成的 `changePct`**，因為那份只留成交值前 300 名，
而法人資料一天有九百多檔。逆勢買超最有意思的常常正是排不進前 300 的中型股 ——
少了它們，畫面上「算不出來」與「沒有逆勢」會長得一模一樣，等於安靜地把答案刪掉一半。
（2026-09-03：934 檔算得出漲跌，前 300 名只蓋到其中 296 檔。）

### 一個沒辦法解決的誤差：除權息

漲跌是**收盤對收盤**算的，沒有還原除權息。官方的「漲跌價差」是對除權息參考價算的，
所以**除權息當天，這裡會把配息算成下跌**，那一檔會出現在逆勢買超榜上而其實只是配息。

要修得有除權息日與配息金額的資料源，那是另一條線。目前的做法是在說明卡裡寫清楚，
並建議金額大的那幾檔回頭確認當天是不是除權息日 —— 錯誤講出來，比安靜地錯好。

順帶測出一件事：拿 `insti/chg/` 去對 `daily/all/` 的官方 `changePct`，2026-09-03
那天 296 檔裡只有 1 檔對不上（3661 世芯-KY，官方那一欄是 0，實際 4200 → 4050 是
-3.57%）—— 是官方那一欄的漲跌價差沒出來，不是這裡算錯。

### 名次那一格擺的是當日漲跌

四張榜裡有兩張是逆勢榜，逆不逆勢全看漲跌那一格。擺在 figures 那一欄的話，讀者得
一列一列往右找，而那一欄在手機上是最先被截掉的。所以成交值名次退到中間的 chip 上，
漲跌接手 `.rank .delta` 那一格 —— 這是全站第一頁這樣用它的，其他頁那一格放的是
名次變化或連續天數，但三者的共通點是「這一列為什麼在這張榜上」。

每一列的最後帶「佔成交值」＝這一邊的買賣超 ÷ 當日成交值，與法人頁同一個理由：
同一個金額套在大小型股上意義完全不同。

### 沒做的事

- **不做跨日累計的買超排行**（近 5 日、近 20 日買超最多）。那是「連買」頁的問題，
  而且累計買超與連續買超是兩種不同的持續性，擺在同一頁會互相稀釋
- **不加金額門檻的 pill。** 四張榜都取前 30、依金額排序，門檻在這裡是多餘的一格 ——
  取前 30 本身就是門檻
- **不把「大盤那天漲還是跌」拉進來。** 逆勢在這一頁是**個股自己的**逆勢；大盤的漲跌
  另有「大盤」頁，兩個混在一起會分不清「逆大盤」與「逆自己」

## P2：導覽分層

> ✅ 已完成。分頁列從「15 個一字排開」改成「6 個群組 + 該群組的分頁」兩層。
> 路由與 15 個 `render*` 函式一行都沒改——變的只有導覽。

**使用者的問題**：功能越加越多，我要的那一頁在哪裡？

15 個分頁擠在一列靠橫向捲動撐著，手機上一次只看得到 6 個，**捲出去的那幾個等於不存在**。
（CSS 註解當時還寫著「現在是九個」，那本身就是已經漂移的訊號。）

### 分組的軸：這一頁回答什麼問題

不是「資料從哪來」。使用者腦中的問題是「誰在買」，不是「這份資料來自集保還是三大法人」。

| 群組 | 分頁 |
|---|---|
| 排行 | 排行 · 站穩 · 異動 |
| 技術 | 爆量 · 均線 · MACD |
| 籌碼 | 大戶 · 法人 · 買超 · 連買 |（「買超」是後來加的，見上面一節）
| 資金 | 族群 · 流向 · 大盤 |
| 環境 | 報價 · 美股 |
| 查詢 | 個股 · 對照 |

### 為什麼不拆成多個 HTML

判準是「**這一頁吃不吃全站共用的狀態（日期＋範圍）**」。實際查過 15 個 `render*`：
幾乎每一個都直接或間接讀 `state.date` / `state.scope`。拆出去的代價是
「切到另一頁就忘記你選的日期跟範圍」——那是每天都會踩到的痛，換來的只是檔案小一點。

`us.html` 之所以適合獨立，正是因為它不吃這組狀態，只讀自己那一份 JSON。
**所以分類要解決的是導覽，不是把檔案拆開。**

### 實作

- `NAV` 在 `app.js` 一處定義群組與分頁；`index.html` 只列六個群組，分頁清單不在 HTML 裡
- `paintNav()` 在 `render()` 最前面呼叫：載入中也要看得到自己在哪一頁
- **群組列 sticky、分頁列不 sticky**：上面已經有 app-bar 與群組列兩層，
  第三層黏在頂上會把手機畫面吃掉三分之一
- 每個群組記住上次停在哪個**完整 hash**（localStorage `stocktracker.nav`）——
  存整串而不是只存 view，回到「查詢」時才會回到原本那一檔個股
- 群組的 `href` 跟著記憶改寫，中鍵開新分頁也是對的
- `us.html` 放一份寫死的靜態導覽（群組列指回 `index.html`），否則獨立頁是死路

### 一個踩到的坑：導覽與篩選長得一樣

分頁列第一版直接沿用既有的 `.pills`，結果「MACD」和它底下的「黃金交叉」都是藍色藥丸，
兩排疊在一起分不出哪一排是換頁、哪一排是篩選。全站改成一條規則：
**導覽用底線，篩選用藥丸**。

---

## P3：篩選與排序

> ✅ 已完成。排行頁三個下拉：篩選（全部／只看自選／排除 ETF／各產業）、
> 排序（依名次／漲跌幅／名次變化）、成交值門檻（不限／50／100／200 億）。
> 純前端，全部改在 `renderRank()` 裡。

## P3：自選股與客製推播

> ✅ 已完成。個股頁有「加入自選」按鈕，清單存在 localStorage（key 為
> `stocktracker.watch`，讀寫都包在 try/catch 裡，無痕模式不會壞）。
> 自選股在所有列表上都標星號，排行頁可以只看自選。
>
> `notify_telegram.py` 新增 `WATCHLIST` 環境變數與 `--watchlist` 參數：
> 設了就只推那幾檔的進出榜動態，當天沒動靜就不發。
>
> ⚠️ 瀏覽器的 localStorage 到不了 GitHub Actions，兩邊**不會自動同步**。
> 排行頁選「只看自選」時會列出可複製的代號字串，手動貼進 secret。
> 要真的同步就得有後端、或把清單 commit 進 repo，兩者都不值得為此破壞靜態架構。

## P3：爆量分頁

> ✅ 已完成。`#/burst`，三個條件同時成立才列出來：
> 成交量大於門檻（預設 2 萬張）、成交量創 N 日新高（預設 60 日）、收盤價大於開盤價。
> 前兩個講量，第三個要求那些量有把價格推上去——只看量會把爆量下殺的出貨也算進來，
> 就是量價四象限右下角那一群。
>
> 第三個條件做成可切換的（只看收紅／不限漲跌），因為那一群本身就值得看：
> 2026-08-28 用 2 萬張＋60 日新高篩出 5 檔，只有 1 檔收紅，
> 另外 4 檔（國喬 21 萬張創 250+ 日新高卻收黑）正是右下角那個象限。
> 開盤價缺漏的日子分不出紅黑，這組 pill 直接不顯示，免得看起來有篩其實沒篩。

### 資料面：既有來源多抓兩個欄位

**開盤價 `open`**：四個來源本來就都有（`OpeningPrice`／`開盤價`／`Open`／`開盤`），
`make_record()` 多存一個欄位就好，不需要新資料源。但**加欄位之前抓的日子沒有這個值**，
前端要能容忍它不存在（`hasOpen` 為假時只篩前兩個條件，並提示怎麼回補）。
要補齊整段歷史得跑 `backfill.py --force`，兩個市場各約 520 個請求。

**成交量新高天數 `vh`**：在 `build_history.py` 用單調遞減堆疊算，攤提 O(1)——
把量比今天小的日子都彈掉，剩下的堆頂就是上一次「量不小於今天」的那天，距離就是新高天數。
平手不算新高。存成每檔一個小整數（`vh` 為 1 的省略不寫，佔一半以上），
前端就不必為了算 60 日新高去抓 60 個 `daily/*.json`（2.6 MB），任何 ≤ 250 的視窗都是免費的。

⚠️ 量的歷史只看得到「當天成交值前 300 名」，沒進榜的日子一律當成量比今天小。
對突然爆量的股票這個假設是對的（它先前連成交值前 300 都排不上），
但**低價高量股**平常量大也排不進成交值前 300 名，新高天數會被高估。UI 有註明。

## P3：均線分頁

> ✅ 已完成。`#/ma`，看的是「剛穿越」而不是「現在站在哪」：
> 5／10／20／60 日線 ×「近 1／3／5／10 個交易日內」的矩陣，每一格是檔數也是選擇鈕，
> 按下去就換下面的清單；最後多一欄「不限」，不看天數、列的是現在就收在那一側的全部。
> 上方兩組 pill：「剛站上／剛跌破」與「不限／四線全上／四線全下」。
> 站上＝收盤價高於均線，跌破＝收盤價不高於均線（剛好相等算在跌破側）；
> 天數只要收回線的另一側就重新起算，所以「近 3 日站上」就是連續站上 1～3 天。
> 每一列還帶四條線各自的位置（▲ 在線上／▼ 在線下／· 資料不足），四個都 ▲ 就是短中長期全在腳下，
> 「四線全上／全下」就是拿這四個標記在篩，與矩陣是 AND 關係（有一條算不出來就不算數）。
> 位置判斷用的是均線價而不是天數：天數在資料剛好夠算均線、卻不夠往回數的日子會是 null，
> 位置那時候仍然是確定的。

### 資料面：前 300 名的收盤價不夠，要另存全市場

均線要的是**連續**的收盤價。`daily/*.json` 只留成交值前 300 名，一檔只要有幾天掉出前 300，
那段就是空的——實測 2026-08-28 榜上前 200 名裡，5 日線只有 168 檔湊得齊，
60 日線只剩 106 檔（要再往回看 10 天判斷穿越的話只剩 101 檔）。

所以抓取端多寫一份 `docs/data/close/{twse,tpex}/YYYY-MM-DD.json`（`{代號: 收盤價}`，
全市場約 2,300 檔、每天約 33 KB，不到 `daily/` 的一半）。四個來源本來就都有收盤價，
不需要新資料源；`backfill.py` 把「排行與收盤價都齊了」才算補過，重跑一次就會補上缺的那半邊。

**均線在 `build_history.py` 算完寫進 `daily/`**：每檔一個 `ma` 陣列（四條線各一個帶正負號的整數，
正＝已站上幾個交易日、負＝已跌破幾天、null＝資料不足）與 `mav`（均線價），只存前 200 名。
前端因此不必為了 60 日線去抓 60 個檔案，跟 `vh` 是同一套思路。
天數最多算到 60（顯示成 60+），再往前對「剛穿越」已經沒有分辨力。

⚠️ 兩個要誠實面對的限制：

- 收盤價一斷（停牌、剛上市、回補還沒補到那麼早）就標「資料不足」，不硬算。
  往回數天數時若是**資料先斷**而不是真的穿越，天數小於 11 天的一律當成資料不足——
  「有資料的這 3 天都站上」不等於「3 天前剛站上」，報成剛站上就是假訊號。
  前端最多回看 10 日，斷在 10 天以外不影響判讀。
- 收盤價沒有還原權值。除權息當天價格跳空往下、均線還帶著除息前的價位，
  那一天的「跌破」可能只是除息造成的。UI 有註明。

## P3：MACD 分頁

> ✅ 已完成。`#/macd`，兩組 pill：「黃金交叉／死亡交叉」與時點
> 「近 1／3／5／10 日（已交叉）／明天／後天」，每個 pill 上直接帶檔數。
> DIF＝12 日 EMA − 26 日 EMA，DEA＝DIF 的 9 日 EMA，柱＝DIF − DEA；
> 柱由負轉正是黃金交叉，剛好為 0 算在死亡交叉那一側。交叉天數與均線頁同一套約定。

### 「明天／後天」是解方程式，不是預測

三條 EMA 都是「舊值 × 常數 ＋ 新收盤價 × 常數」，所以「明天的柱等於 0」對明天的收盤價是線性的，
臨界價直接解得出來：

```
P* = (DEA − [(1−α₁₂)·EMA₁₂ − (1−α₂₆)·EMA₂₆]) / (α₁₂ − α₂₆)
```

清單上的「明天收 ≥ 127.40（−2.75%）」就是這個解與它離今日收盤的距離。**負的代表連平盤或
下跌都會交叉**，那是最接近成真的一群，所以排序用「要走的幅度」由小到大。

「後天」多一個假設：**明天以平盤計**（兩天兩個未知數，不假設一個就解不出來），並排除掉
「明天平盤就會交叉」的那些——它們是「明天」那一格的事。兩邊都只列漲跌停 ±10% 走得到的，
ETF 與部分商品沒有這個限制，門檻對它們保守了一點。

### 資料面：與均線共用同一份收盤價

`daily/` 裡每檔多一個 `macd`：`[快線 EMA, 慢線 EMA, DEA, 交叉天數]`。存三條 EMA 而不是
DIF／DEA／柱，前端才推得出上面那條臨界價；EMA 存到小數第 3 位，臨界價的分母是
α₁₂ − α₂₆ ≈ 0.08，誤差會放大約 12.5 倍，3 位小數換算到價格是 0.006 元，看得見的位數都還是準的。

⚠️ EMA 沒有真正的起點（從第一天的收盤價遞推），連續收盤價少於 60 個交易日就不出數字——
26 日 EMA 的起點誤差到那時只剩約 1%。因此 MACD 的可用天數比均線少：同樣一份收盤價，
均線回推得到 110 天，MACD 只有 56 天，要往前看就得再回補。

## P3：法人跨日累計（近 5 日與 20 日）

> ✅ 已完成。不是新分頁，而是給「買超」頁加上**第二排 pill**：當日／近 5 日／
> 近 20 日。四張榜的形狀完全不變，只有數字的來源換成 `insti/sum/`，
> 逆勢的定義跟著期間走（當日是「買超而收黑」，跨日是「累計買超而期間下跌」）。

**使用者的問題**：這幾天下來，法人到底買了多少？

「買超」頁已經有三邊法人的**當日**分項，缺的是跨日累計：外資／投信／自營各自在近
5 日、近 20 日的買賣超合計。做成分頁還是做成軸，最後選了軸——那一頁本來就在問
「誰買最多」，期間只是同一個問題的另一個刻度，拆成兩頁會讓人在兩張長得一樣的
榜之間來回比對。

「買超」那一節的「沒做的事」寫過不做跨日累計，理由是「累計買超與連續買超是兩種
不同的持續性，擺在同一頁會互相稀釋」。那個理由只否定了**擺在買超頁**，沒有否定
這件事本身，所以它自成一項。

**累計買超不等於連續買超**，兩個都要：一檔可以在 20 個交易日裡累計買超 50 億，
而中間有 8 天是賣的——「連買」頁看不到它（連續性斷了），累計榜看得到。反過來，
一檔連買 12 天但每天只有幾千萬，連買頁排得很前面，累計榜上根本不在。

### 資料面：後端算，與「連買」同一個理由

近 20 日累計要回頭讀 20 個 43 KB 的每日檔，讓前端自己抓是 800 KB 的代價。所以照
`insti/streak/` 的做法，`build_institutions.py` 一天寫一個
`insti/sum/{日期}.json`：每一檔三邊 × 兩種窗（5／20 日）的累計金額、累計股數與
期間漲跌。純衍生資料，由 `insti/daily/` 重算，不重抓任何 API。

### 做完之後改掉的兩個設計

**一、`days` 放錯層級。** 第一版把「實際算了幾個交易日」存在每一列上，跑完才發現
同一天同一個窗口裡每一檔的天數都一樣——它是窗口的性質，不是個股的性質。
移到 `w.{窗口}.days`。

**二、單一金額門檻有系統性偏差。** 第一版沿用 `daily/` 那招「三邊的估算金額都不到
0.3 億就不存」，結果檔案 125 KB（比 `daily/all` 還大，對一份衍生便利檔不合比例）。
更糟的是**外資的金額比投信大一個量級**，拿 `max(三邊)` 去砍，砍掉的幾乎都是投信
有意思的中型股——而那正是這張榜該挑出來的東西。

改成按榜取：**每個市場、每一邊各留前 100 名**（四邊＝畫面上的四個 pill，含三邊
相加的三大法人），絕對值不到 0.1 億的不留。畫面只排前 30，所以榜尾不可能因為檔案
的邊界而憑空少幾檔；分市場取是為了範圍選單，某檔若排得進合併後的前 N 名，它在
自己市場裡必然也在前 N 名內。**125 KB -> 53 KB，而且是往「少砍掉訊號」的方向砍的。**
實測 2026-09-03 的 20 日窗口：上櫃投信留了 72 檔（全市場就只有這麼多超過 0.1 億），
其餘七個市場 × 邊的組合都在 127~198 檔，每一種切法都排得滿前 30。

### 跨日沒有「佔成交值」

當日那一檔每一列的最後有「佔成交值」，跨日的兩檔沒有：單日的成交值不能當 N 日累計
的分母，而本站沒有存 N 日累計成交值。與其擺一個分母是錯的百分比，不如那一格不要。

### 資料起點的硬限制

本站的法人資料從 2026-08-05 開始。20 日累計在資料起點附近算不滿，而**算不滿的
累計看起來就跟「那段時間法人沒什麼動作」一模一樣**。所以窗口不足時要標，沿用
連買頁「連 N+ 天」的同一個慣例——寧可畫面上多一個符號，也不要安靜地少算。

---

## P3：力道標（今日買賣超是平常的幾倍）

> ✅ 已完成。每一列的張數後面多一格「力道 N 倍」，並且成為「買超」頁的**第三個軸**
> ——當日那個期間多一排 pill：依金額／依力道。

**使用者的問題**：今天這筆買超，對這一檔來說算大嗎？

「買超」頁每一列已經有「佔成交值」，它回答的是「相對於**今天的量**大不大」。
力道標回答的是另一個問題：「相對於**這一檔自己的平常**大不大」。

兩個都需要。一檔平常每天被外資買賣幾百萬、今天忽然買超 2 億，佔成交值可能只有
3%（因為今天量也放大了），但對這一檔來說是空前的。反過來，權值股天天被買超
十幾億，佔成交值 1%、力道 1.1 倍——兩格一起看才知道今天有沒有異常。

### 定義：除以中位數，不是平均

    力道 ＝ 今日買賣超金額 ÷ 過去 20 日該檔買賣超金額絕對值的「中位數」

用中位數不用平均：平均會被過去 20 天裡某一天的大額整個吃掉，算出來的「平常」
其實是那一天。

### 分母接近零是這一項最大的坑，但「藏起來」是錯的解法

平常沒人動的個股，分母趨近 0，今天動一次就是 50 倍、200 倍——那個數字看起來
最聳動，資訊量卻最低。

原本規劃的第一道閘是「**中位數不到 0.1 億就不給力道標，那一格留白**」。
**實測否決了它**：1,445 檔曾進過每日檔的個股裡，570 檔的外資日金額中位數是 0
（大部分交易日根本沒進檔案），再加 98 檔不到 0.05 億——留白等於對 58% 的個股
交白卷，而那裡面正好是最有意思的一類（平常沒人碰、今天忽然有人買三億）。

改成**夾**：分母取 `max(中位數, 0.05 億)`。0.05 億是 `insti/daily/` 自己的收錄門檻，
比它小的金額檔案裡根本沒有記錄，所以那是這份資料的**解析度下限**——真正的中位數
是多少我們不知道，只知道在那之下。拿它當地板，等於說「**最多**只能講到這個倍數」，
而不是假裝那一格沒有答案。

第二道閘保留，但只套在**依力道排序**上：金額不到 0.5 億的不進力道榜。
徽章本身不設金額門檻——地板已經讓倍數與絕對金額綁在一起了。

### 資料面：另出一份 `insti/base/`，不是併進 `insti/sum/`

原本規劃「與跨日累計共用同一份檔，多存一欄中位數」。做的時候發現不行：
`insti/sum/` 只留「每個市場每一邊前 100 名」，約 400 檔；而力道標要掛在**當日**
那張榜的每一列上，當日的母體是 935 檔。

更糟的是漏掉的那些正是重點：一檔今天買超 10 億、而過去幾天賣掉 10 億的股票，
20 日淨額接近 0、進不了 `sum/`——**但它恰恰是力道最高的那一類**。所以另寫一份
`insti/base/{日期}.json`（約 21 KB／天），母體與 `insti/daily/` 對齊。

存的是**分母**不是倍數：同一份檔案日後改顯示方式（例如改成分位數）不必重算歷史。

### 「三大法人」的分母必須另外算

中位數不可加。某一檔可能外資與投信天天一買一賣、各自的中位數都是 3 億，而三邊
相加後的淨額中位數只有 0.2 億。實測台積電：三邊中位數相加 104.8 億，
三大法人淨額的中位數只有 89.4 億；欣興是 28.0 對 21.2。拿相加當分母，
力道標會系統性偏小兩三成。所以 `base` 存四個值，第四個是把每一天的三邊先相加
再取絕對值算出來的。

### 統計母體不能被排序汙染

第一版把力道排序做成 `legPick()` 的一個模式，結果上方統計格跟著變：切到依力道之後
「外資買超」從 303 檔變成 105 檔——那 105 只是通過 0.5 億門檻的檔數，不是買超檔數。

改成兩層：`legPick()` 算母體（永遠依金額），`byForce()` 只負責重排顯示用的那一份。
統計格一律取自母體。

### 跨日的兩個期間沒有力道標

要算「5 日（或 20 日）累計的平常是多少」，得有 20 個 20 日累計的樣本，也就是回頭看
40 個交易日以上——本站的法人資料目前只有 22 個交易日。這是「**還不夠**」不是
「做不到」，資料長到那裡就補得上。同理，資料起點往後那 10 天前面湊不滿 10 個
交易日，`insti/base/` 整天不寫檔（22 天裡只有 12 天有）。

---

## P3：法人均價（買進與賣出價位）

> ✅ 已完成**排行那一半**：跨日的兩個期間，每一列的最後是「買均／賣均」。
> K 線圖上疊一條均價線的那一半**移到**
> [個股的法人歷史](#p3個股的法人歷史)——兩者要的是同一份東西（一檔股票的
> 逐日法人資料），拆在兩項裡做會把同一份轉置檔建兩次。

**使用者的問題**：法人這一批，大概買在什麼價位？

### ⚠️ 這一項的誤差是結構性的，做之前要先接受

官方的個股法人資料**從頭到尾只有股數**，本站的金額一律是「買賣超股數 × 當日
收盤價」估算出來的（見「法人」一節）。拿這個估算金額去回推均價，等於**用收盤價
當均價、再除回來**——算出來的東西就是「以買賣超股數加權的收盤價平均」，
它不是法人的成本。

當天振幅越大誤差越大；真正的成交均價在盤中，日報表裡沒有任何一欄摸得到它。

所以能做的只有一個誠實的版本：**買超期間的股數加權收盤均價**，而且畫面上
**不可以叫它「法人成本」或「法人成本線」**。要嘛這樣標，要嘛不做——標成成本線
會讓使用者拿它去算「法人套住了沒」，那個結論建立在一個假的數字上。

### 規劃時寫錯的一句：「累計金額 ÷ 累計股數就是」

這一節原本寫「工程量的重點不在算，`insti/sum/` 的累計金額 ÷ 累計股數就是」。
**那句話是錯的。** 那個商數看起來就是均價，其實不是：淨額是**相減**的結果，
拿它當分母沒有物理意義。

實測近 20 日有外資淨額的 1,412 檔，**只有 228 檔期間內是單邊**（只買或只賣），
其餘 1,184 檔有買有賣。用淨額算出來的「均價」有 219 檔落在期間的價格區間外：

| | 淨額直接除 | 那 20 天的收盤區間 |
|---|---|---|
| 竹陞科技 | **-4,849 元** | 960 ~ 1,180 |
| 台積電 | 2,597 | 2,350 ~ 2,440 |
| 信驊 | 12,476 | 15,115 ~ 17,850 |

改成只取**與淨額同方向的那幾天**：淨買超只算買進的那幾天、淨賣超只算賣出的那幾天。
權重全部同號，結果必定落在那幾天的收盤區間內（實測 357 檔 × 4 邊共 1,315 個值，
超出區間的 0 個；驗證時出現的 3 個邊界例外是 1e-15 等級的浮點誤差，四捨五入就沒了）。

所以它**不能**由前端從既有欄位除出來，得在後端另外算四個值（三邊 + 三大法人，
均價同樣不可加）。`insti/sum/` 因此從 53 KB 長到 71 KB。

### 它描述的是那幾天，不是淨額

一檔買 10,000 張、賣 9,900 張的股票，淨額只有 100 張，但買均描述的是那 10,000 張。
所以畫面上那一格叫「買均／賣均」而不是「均價」。

### 命名的那條線真的守住了

頁面上刻意**不做**「現價 vs 均價」的價差百分比。現價就在同一列上，要比自己比，
但站方不把那個減法算給你看 —— 一旦算了，它就變成「法人套住了沒」，而那個結論
建立在一個假的數字上（見上面的結構性誤差）。說明卡裡直接寫死「它不是法人的成本」。

---

## P3：個股的法人歷史

> ✅ 已完成。個股頁多一張「三大法人買賣超」卡（三邊堆疊長條圖 + 區間累計 6 格），
> 並在 K 線圖上疊一條「外資 20 日買均」虛線。資料是後端轉置好的
> `insti/stock/{代號}.json`。

**使用者的問題**：這一檔過去幾週，法人是怎麼進出的？

這一項現在也含**走勢圖均價線**（原本在「法人均價」那一項）：K 線圖上疊一條法人
買均的水平線。移過來的理由是兩者要的是同一份東西 —— 一檔股票的逐日法人資料。

現在要翻任一天的法人買賣超，得靠頂部的日期選單，而且看到的是**那一天的全市場
榜**，不是**這一檔的歷史**。個股頁應該有一張「三大法人逐日買賣超」表，加一張
把三邊疊起來的圖。

### 資料面：這是轉置問題，`history/` 已經解過一次

`insti/daily/` 是「一天 → 所有股票」，個股頁要的是「一檔 → 所有天」。成交值排行
遇過一模一樣的問題，解法是 `history/{範圍}/{年}.json` 那份轉置表；集保那邊也有
`holders/stock/{代號}.json`。

照 `holders/stock/` 的做法寫 `insti/stock/{代號}.json`，九百多檔各一個檔，
量級與集保那份相同，可行。純衍生，由 `insti/daily/` 重算。

### 切檔方式：一檔一個，不切年也不截斷

這是做之前唯一要定的事，最後選了「跟 `holders/stock/` 一樣」：

- **不照 `kline/` 切月**：個股頁的期間選單有「全部」，切月的話那個選項要抓十幾個
  檔；而法人資料一檔一年才約 7.5 KB，整段抓回來一次反而便宜
- **不截斷成最近 N 天**：同樣因為有「全部」這個選項
- 實測：1,445 檔、22 個交易日共 828 KB（平均 587 B／檔），外推一年約 9 MB
  ——對照 `holders/stock/` 的 1.8 MB，日資料對週資料，比例合理
- 日後真的長到不能一次抓，`d` 是獨立的日期陣列，切年很容易

用 `write_if_changed` 寫（與 `build_holders.py` 同一招）：每次都從 daily/ 重算全部
一千多檔，直接寫的話每天就是一千多個檔的差異，而其中有幾百檔今天根本沒動。

### 均價線與買超頁必須是同一個定義

個股頁的買均在前端由序列現算，買超頁的買均在後端寫進 `insti/sum/`。兩邊算出不同的
數字是最難發現的那種錯，所以前端那一支刻意照抄 `avg_prices()` 的規則：只取**與淨額
同方向的那幾天**的股數加權收盤均價，不是累計金額 ÷ 累計股數。
驗過：台積電兩邊都是 2,414.69、欣興都是 1,085.99。

### 稀疏的日期軸要講出來

`d` 只有「有進每日檔」的那幾天（三邊都不到 0.05 億的不收），所以近 60 個交易日的
期間裡可能只有 22 天在圖上。缺的那幾天不是沒有資料，是那天法人動的是零頭——
卡片上直接寫出「近 N 個交易日裡有 M 天」與這句解釋。

同理，均價那條線湊不滿 20 天時標成「20**−** 日」並在說明裡寫出實際天數：
少算幾天的均價看起來跟滿 20 天的一模一樣，而本站法人資料才開始沒多久，
大部分個股都湊不滿。

---

## P3：具名籌碼異動（土洋同買／對作）

> ✅ 已完成三件事：法人頁多兩張**對作**榜、排行頁與個股頁的列上多一個**徽章**、
> Telegram 盤後推播多一段**籌碼異動**。三處與推播共用同一條規則
> （`instiTagOf()` 與 `insti_tag_of()`），算出來的名單必須一樣。

**使用者的問題**：今天有哪幾檔是土洋同買，哪幾檔外資與投信在對作？

**土洋同買／同賣已經有了**，就是「法人」頁那張主榜（市場上把它叫土洋同買，
本站叫「外資投信同買」）。這一項要補的是另外兩件事：

1. **對作**：外資買、投信賣（或相反）。目前站上沒有。它有意思的地方跟同買是同一個
   ——兩種完全不同的決策流程，這次指到了**相反**的方向。誰對誰錯事後才知道，
   但它標示的是「這一檔現在有分歧」，那本身就是資訊
2. **徽章與推播**：把「同買」「對作」變成排行榜與個股頁列上的一個小標（沿用既有
   `.chip` 的視覺語言），並讓 `notify_telegram.py` 在盤後推播當天的名單。推播的
   架構已經有了（現在推的是新進榜與連續 2、3、5 天），加一段不用新東西

### 對作要依「較小的那一邊」排序

同買榜依合計排序，對作不能照抄：對作的合計接近零（兩邊互相抵消），拿它排序等於
隨機。真正決定「這場對作有多實在」的是**較小的那一邊**——外資買 50 億配投信賣
0.5 億，與外資買 5 億配投信賣 5 億，後者才是兩邊都下了重手。

門檻同樣是「兩邊各自都要達到」。對作這邊尤其必要：外資買超十億配上投信賣超三百萬，
那不是對作，那是外資在買而投信沒動。

### 對作不是邊角案例

實測 2026-09-03、門檻 0.5 億：同買 22 檔、同賣 14 檔，而**對作 39 檔**（外資買
投信賣 17、外資賣投信買 22）——比同買同賣加起來還多。它是同買的補集裡唯一講得出
內容的那一塊（只有一邊在動的那些，看不出任何東西）。

### 排行頁的徽章值不值一次 43 KB

徽章真正有用的地方是**法人資料沒有直接顯示**的頁面，也就是排行頁；在法人頁與買超頁
上三邊的金額本來就攤開在列上，徽章是多餘的。所以排行頁順便載 `insti/daily/`
（43 KB，與法人頁共用同一份快取），用 `.catch(() => null)` 包起來——那一天沒有法人
資料就是沒有徽章，整頁照常。實測 2026-09-03 的前 200 名裡有 67 檔帶徽章。

考慮過另寫一份極小的 `insti/tag/{日期}.json`（約 3 KB）避開那 43 KB，最後沒做：
門檻是可調的（0.3／0.5／1／3 億），要嘛存四份、要嘛存「撐到第幾檔門檻」再由前端還原，
兩種都是為了省 40 KB 而多一份檔案格式。排行頁本來就要載 200 KB 左右，多這一份
在同一個 `Promise.all` 裡平行抓，不影響首次繪製。

### ⚠️ 徽章離「訊號」只有一步

全站的原則是不提供買賣訊號。徽章的文案必須是**描述**（「外資買、投信賣」）
而不是**判斷**（「注意進場」「留意賣壓」）。這條線在這一項上特別容易越過，
因為徽章天生看起來就像提醒。

實際落地的文案：徽章本身只有「土洋同買／土洋同賣／對作·外資買／對作·外資賣」，
個股頁那一行展開成「外資與投信都買超」「外資賣超、投信買超」——全部是事實陳述，
沒有一個字在講該怎麼做。推播那一段結尾固定加一句「金額為估算，非官方數字」。

---

## P4：籌碼異動監控（自選股不限檔數）

> ✅ 已完成。`WATCHLIST` 那一封從「只講進出榜」變成「進出榜 + **逐檔的籌碼動靜**」，
> 並且修掉一個會讓整封信不發的缺口。

**使用者的問題**：我在意的那幾檔，籌碼有動靜嗎？

商用 App 把「能監控幾檔」做成付費階梯，是因為他們得為每個使用者在伺服器上跑掃描。
**這裡沒有伺服器**：排程對全市場跑一次，使用者選幾檔完全不影響成本。
所以「不限檔數」在這個架構下不是功能，是預設。

### 那個取捨：維持 Secret，不寫進 repo

自選股只存在瀏覽器的 localStorage 裡，GitHub Actions 看不到。兩條路各有缺點：

- 存進 repo 的 `watch.json`：簡單，但**誰在看哪幾檔會永久留在 git 歷史裡**
- 存進 GitHub Secret：不公開，但每次改自選股都要手動更新

**選了 Secret**（也就是維持現狀），判準是「哪一個缺點是不可逆的」：寫進 repo 之後
就算改回來，歷史上那一筆也撤不掉；Secret 的缺點只是麻煩。排行頁選「只看自選」時
本來就會列出可以直接複製的代號字串，貼一次就好。

所以這一項**沒有新的資料流**，真正要修的是別的東西。

### 真正的缺口：送不送信只看進出榜

`WATCHLIST` 那一封原本的判斷是「自選股有沒有進出榜」，沒有就整封不發。於是自選股
有籌碼異動、卻剛好沒進前 200 名時，**整封信不會發** —— 而那正是最該通知的情況
之一。實測 2026-09-03 有土洋標記的 75 檔裡，有 8 檔不在前 200 名（遠東新、華南金、
台中銀…）。修法是把籌碼事件也算進「有沒有動靜」。

### 四種事件，全部以外資為主軸

| 事件 | 觸發 |
|---|---|
| 土洋標記 | 同買／同賣／對作，門檻與網站一致（0.5 億） |
| 連買連賣 | 3 天以上（`insti/streak/` 只留 3 天以上的） |
| 力道 | 2 倍以上才算一則事件 |
| 逆勢 | 外資買而股價收黑、或外資賣而股價收紅 |

一檔有任何一項就進訊息，一行講完；事件多的排前面（一次動到三件事的比只動一件的
值得先看），同樣多才比外資金額。衍生檔缺哪一份就少哪一種事件（資料起點附近沒有
`insti/base/`，那幾天就沒有力道）——不是錯誤，是那一天還算不出來。

### 「不限檔數」在這個架構下不是功能，是預設

商用 App 把「能監控幾檔」做成付費階梯，是因為他們得為每個使用者在伺服器上跑掃描。
這裡沒有伺服器：排程對全市場算一次，使用者選幾檔完全不影響成本。

---

## P4：籌碼雷達（多條件排行與歷史回看）

**使用者的問題**：同時符合好幾個籌碼條件的是哪幾檔？

把前面幾項組起來的終點：一個能下多條件的篩選器，例如
「外資連買 5 天以上 **且** 投信也買超 **且** 今日逆勢 **且** 力道 3 倍以上」，
並且能對**過去任一天**跑同一組條件。

這是整條籌碼線工程量最大的一項，理由有兩個：

1. **它要等 P3 那幾項都在**。連買（✅）、逆勢（✅）、跨日累計、力道標——少一個，
   雷達就少一個維度
2. **前端目前沒有條件組合器**。全站的篩選都是單一維度的 pill（一次選一個門檻），
   多條件要的是另一種互動：條件可加可減、結果即時更新、而且要看得出「是哪一條
   條件把清單砍到剩 3 檔」

### 歷史回看的代價要先算清楚

對單一天跑條件很便宜（抓那一天的幾份檔就好）。但「過去 20 天每天都跑一次」
要抓 20 份 × 好幾種檔，那是幾 MB。所以歷史回看只能是**一次跑一天**
（換日期重跑），或者後端再出一份預先算好的寬表——後者等於把雷達的條件寫死在
後端，跟「可自由組合」互相矛盾。

先把 P3 那幾項做完再回來評估，那時才知道寬表要寬到什麼程度。

## P4：上櫃（TPEx）涵蓋

> ✅ 已完成，做成**三種範圍**而不是二選一：全部（上市＋上櫃合併重新排名，預設）、
> 上市、上櫃。三者各自計算名次、進出榜與連續進榜天數。

實作重點與當初的決策：

- **原本的岔路是「合併成一個榜」還是「分成兩個榜」，最後三個都做。**
  兩種問題都成立 ——「今天全市場最熱的是誰」要合併榜，「上櫃裡誰最熱」要分開榜。
  同一檔在「上櫃」排第 3、在「全部」排第 19，是兩件事。
- **上市原有的資料與連續進榜天數完全不受影響**，只是搬到 `daily/twse/`。
  合併榜是新的一套數字，從頭算起。
- **合併榜不必另外抓資料**：某檔若排得進合併後的前 300 名，它在自己市場裡必然
  也在前 300 名內，所以用兩邊各自的前 300 名就能還原正確的合併排行。
  由 `build_history.py` 算出來寫成 `daily/all/`。
- 上櫃的歷史 API（`dailyQuotes`）每次回應約 1.4 MB，含權證共上萬列，
  比上市慢得多，兩年份回補約需一小時。權證由既有的 `is_tracked_code` 濾掉。
- 產業別對照同步擴充到上櫃（`mopsfin_t187ap03_O`），兩個交易所用的是同一套
  產業代碼，只是欄位名稱不同。目前共 1979 檔、35 個產業。
- 代價：`docs/data/` 從 20 MB 長到約 60 MB，每個交易日新增三個檔案而不是一個。

## P4：進榜後表現統計

已經有 485 個交易日的全市場排名與收盤價，可以統計新進榜的股票在 +1／+5／+20 日的價格變化分布，
以及「站穩 5 天」與「一日行情」的差異。這是別的網站沒有的東西。

⚠️ 呈現方式必須是**歷史統計描述**，不能寫成訊號或建議。此頁需要再次標示「資料僅供參考，非投資建議」。

## P5：匯出與分享

CSV 下載（前端組字串 + Blob，不需要後端）、分享連結按鈕。目前完全沒有帶走資料的方法。

## P5：週／月維度排行

目前所有維度都是「日」。可以加：本週累計成交值排行、本月進榜次數最多、本月新進榜清單。

---

## 明確不做的事

- 不加登入、不加後端 API、不加資料庫——沒有伺服器的靜態架構是這個專案的核心優點
- 不做盤中即時報價（資料源是收盤行情）
- 不提供買賣訊號或投資建議
